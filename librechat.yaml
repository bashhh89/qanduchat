version: 1.3.0
endpoints:
  custom:
    - name: pollinations
      apiKey: none
      baseURL: https://text.pollinations.ai/openai
      models:
        default: [openai, mistral, claude-hybridspace, openai-large]
      requiresKey: false
